# 🚀 克隆并发布功能使用指南

## 功能概述

这个功能允许你：
1. **克隆**别人的GitHub仓库
2. **自动修改包名**（添加`bachai`前缀，避免包名冲突）
3. **上传到你的组织**GitHub仓库
4. **自动触发CI/CD**，立即打包发布到包源（PyPI/NPM）
5. **发布到EMCP平台**

## 使用方法

### 方式一：GUI界面（推荐）

1. **启动GUI**
   ```bash
   python repoflow_ultimate.py
   ```

2. **切换到"克隆仓库"标签页**
   - 在界面顶部点击 `🔗 克隆仓库` 标签

3. **填写信息**
   - **GitHub 仓库URL**: 输入要克隆的仓库地址
     - 例如：`https://github.com/username/awesome-mcp`
   - **包名前缀**: 默认为 `bachai`，会自动添加到包名前面
     - 原包名：`awesome-mcp` → 新包名：`bachai-awesome-mcp`
   - **目标组织**: 你的GitHub组织名称（例如：`BACH-AI-Tools`）

4. **点击"克隆并发布"按钮**

5. **等待完成**
   - 会显示实时进度和日志
   - 整个过程约3-5分钟（取决于仓库大小和网络速度）

### 方式二：命令行

```bash
# 基本用法
python clone_and_publish.py https://github.com/user/awesome-mcp

# 自定义前缀
python clone_and_publish.py https://github.com/user/awesome-mcp --prefix myprefix

# 指定输出目录
python clone_and_publish.py https://github.com/user/awesome-mcp --output ./repos

# 完整示例
python clone_and_publish.py \
    https://github.com/user/awesome-mcp \
    --prefix bachai \
    --output ./my_repos
```

## 自动化流程

整个流程完全自动化，包含以下步骤：

```
1️⃣  克隆源仓库
    ↓
2️⃣  检测项目类型（Python/Node.js）
    ↓
3️⃣  获取原始包名
    ↓
4️⃣  修改包名（添加前缀）
    ├─ Python: 修改 pyproject.toml / setup.py
    └─ Node.js: 修改 package.json
    ↓
5️⃣  扫描敏感信息
    ↓
6️⃣  创建GitHub仓库（在你的组织下）
    ↓
7️⃣  生成CI/CD Pipeline
    ├─ Python: 生成 PyPI 发布工作流
    └─ Node.js: 生成 NPM 发布工作流
    ↓
8️⃣  配置GitHub Secrets（PyPI/NPM Token）
    ↓
9️⃣  推送代码到GitHub
    ↓
🔟 立即创建Tag，触发发布
    ↓
1️⃣1️⃣ 等待包发布到包源
    ↓
1️⃣2️⃣ AI生成多语言描述
    ↓
1️⃣3️⃣ 生成Logo（即梦MCP）
    ↓
1️⃣4️⃣ 发布到EMCP平台
    ↓
1️⃣5️⃣ 可选：MCP测试
    ↓
1️⃣6️⃣ 可选：Agent测试
    ↓
✅ 完成！
```

## 配置要求

在使用前，请确保 `config.json` 中配置了以下信息：

```json
{
  "github": {
    "token": "你的GitHub Token",
    "org_name": "你的组织名称"
  },
  "pypi": {
    "token": "你的PyPI Token（如果是Python项目）"
  },
  "npm": {
    "token": "你的NPM Token（如果是Node.js项目）"
  },
  "emcp": {
    "phone_number": "EMCP登录手机号",
    "base_url": "https://sit-emcp.kaleido.guru"
  },
  "azure_openai": {
    "endpoint": "Azure OpenAI端点（可选）",
    "api_key": "API密钥",
    "deployment_name": "部署名称"
  },
  "jimeng": {
    "enabled": true,
    "mcp_json": {
      "mcpServers": {
        "jimeng": {
          "url": "http://mcptest013.sitmcp.kaleido.guru/sse",
          "headers": {
            "emcp-key": "你的key",
            "emcp-usercode": "你的usercode"
          }
        }
      }
    }
  }
}
```

## 示例场景

### 场景1：克隆一个Python MCP工具

```bash
# 源仓库: https://github.com/awesome-user/data-analysis-mcp
# 原包名: data-analysis-mcp
python clone_and_publish.py https://github.com/awesome-user/data-analysis-mcp

# 结果:
# ✅ 新包名: bachai-data-analysis-mcp
# ✅ 新仓库: https://github.com/BACH-AI-Tools/bachai-data-analysis-mcp
# ✅ PyPI: https://pypi.org/project/bachai-data-analysis-mcp/
# ✅ EMCP: 已发布模板
```

### 场景2：克隆一个Node.js MCP工具

```bash
# 源仓库: https://github.com/cool-dev/web-scraper
# 原包名: web-scraper
python clone_and_publish.py https://github.com/cool-dev/web-scraper

# 结果:
# ✅ 新包名: bachai-web-scraper
# ✅ 新仓库: https://github.com/BACH-AI-Tools/bachai-web-scraper
# ✅ NPM: https://www.npmjs.com/package/bachai-web-scraper
# ✅ EMCP: 已发布模板
```

## 包名规则

### Python项目
- **原包名**: `my-package`
- **新包名**: `bachai-my-package`
- **安装命令**: `uvx --from bachai-my-package@1.0.0 bachai_my_package`

### Node.js项目
- **原包名**: `my-package`
- **新包名**: `bachai-my-package`
- **安装命令**: `npx bachai-my-package`

### 带Scope的NPM包
- **原包名**: `@org/my-package`
- **新包名**: `@org/bachai-my-package`（保留scope）

## 注意事项

1. **GitHub Token权限**
   - 需要有创建仓库的权限
   - 需要有设置Secrets的权限
   - 需要有推送代码的权限

2. **包源Token**
   - Python项目需要PyPI Token
   - Node.js项目需要NPM Token
   - Token会自动设置到GitHub Secrets中

3. **包名冲突**
   - 前缀功能可避免与原包名冲突
   - 建议使用有意义的前缀（如公司/团队名）

4. **首次发布**
   - 克隆后会立即创建Tag触发发布
   - GitHub Actions通常需要2-3分钟完成打包
   - 工具会自动等待包发布成功

5. **临时目录**
   - 克隆的仓库默认保存在临时目录
   - 可以通过 `--output` 参数指定保存位置
   - 完成后可手动清理临时目录

## 故障排查

### 问题1：克隆失败
- **检查**: 源仓库URL是否正确
- **检查**: 网络连接是否正常
- **检查**: 是否有权限访问该仓库

### 问题2：包名修改失败
- **原因**: 项目结构不符合标准
- **解决**: 确保项目有 `pyproject.toml`、`setup.py` 或 `package.json`

### 问题3：GitHub推送失败
- **检查**: GitHub Token是否有效
- **检查**: 组织名称是否正确
- **检查**: 是否有推送权限

### 问题4：包发布失败
- **检查**: GitHub Actions日志
- **检查**: PyPI/NPM Token是否有效
- **检查**: 包名是否已存在（应该不会，因为有前缀）

### 问题5：EMCP发布失败
- **检查**: EMCP账号配置是否正确
- **检查**: 验证码是否正确
- **检查**: 网络连接是否正常

## 技术细节

### 包名修改原理

**Python项目**:
```python
# 原 pyproject.toml
[project]
name = "awesome-mcp"

# 修改后
[project]
name = "bachai-awesome-mcp"
```

**Node.js项目**:
```json
// 原 package.json
{
  "name": "awesome-mcp"
}

// 修改后
{
  "name": "bachai-awesome-mcp"
}
```

### GitHub Actions自动触发

创建Tag时会自动触发对应的发布工作流：
- Python: `.github/workflows/pypi-publish.yml`
- Node.js: `.github/workflows/npm-publish.yml`

工作流会自动：
1. 构建包
2. 运行测试
3. 发布到包源

## 总结

这个功能完美解决了你的需求：

✅ **需求1**: 克隆GitHub仓库 - ✓ 支持  
✅ **需求2**: 上传到组织 - ✓ 自动完成  
✅ **需求3**: 修改包名添加前缀 - ✓ 自动添加  
✅ **需求4**: 首次推送立即打包发布 - ✓ 自动触发  

整个流程完全自动化，只需一键操作即可完成！🎉





