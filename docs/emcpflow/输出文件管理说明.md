# 输出文件管理说明

## 📁 目录结构

从现在开始，所有生成的输出文件都会自动保存到 `outputs` 目录：

```
outputs/
├── logos/          # Logo 文件
│   ├── logo_package1.png
│   ├── logo_package2.png
│   └── ...
└── reports/        # HTML 测试报告
    ├── mcp_test_report_xxx.html
    ├── agent_chat_test_xxx.html
    └── ...
```

## 🎯 优势

### 1. **项目更整洁**
- ✅ 所有输出文件集中管理
- ✅ 项目根目录保持干净
- ✅ 易于查找和管理

### 2. **Git 友好**
- ✅ `outputs/` 目录已添加到 `.gitignore`
- ✅ 生成的文件不会被提交到 Git
- ✅ 避免仓库体积膨胀

### 3. **自动创建**
- ✅ 目录不存在时自动创建
- ✅ 无需手动维护
- ✅ 开箱即用

## 📦 文件说明

### Logo 文件 (`outputs/logos/`)

**格式**：`logo_{包名}.png`

**来源**：
- 即梦 MCP Logo 生成器
- DALL-E 生成的 Logo
- 从包管理平台获取的 Logo

**使用场景**：
- 发布到 EMCP 平台时上传
- 项目展示和文档
- 宣传材料

**示例**：
```
logo_bachai-data-analysis-mcp.png
logo_google_search74.png
logo_jsearch.png
```

### HTML 测试报告 (`outputs/reports/`)

#### MCP 测试报告

**格式**：`mcp_test_report_{随机ID}.html`

**内容**：
- 所有工具的测试结果
- 成功率统计
- 详细的调用日志
- EdgeOne CDN 分享链接

**查看方式**：
- 用浏览器直接打开
- 或访问 EdgeOne 公开链接

**示例**：
```
mcp_test_report_e71c0536.html
```

#### Agent 对话测试报告

**格式**：`agent_chat_test_{会话ID}.html`

**内容**：
- 对话测试流程
- 工具调用记录
- AI 回复内容
- 测试统计信息

**示例**：
```
agent_chat_test_31f63615.html
```

## 🧹 清理现有文件

如果你有旧的 logo 和 HTML 报告文件散落在项目根目录，可以运行清理脚本：

### Windows (PowerShell)

```powershell
.\cleanup_outputs.ps1
```

### Linux / macOS

```bash
chmod +x cleanup_outputs.sh
./cleanup_outputs.sh
```

### 清理结果

```
🧹 开始整理输出文件...
📁 创建输出目录...
   ✅ 已创建 outputs/logos
   ✅ 已创建 outputs/reports

📦 移动 logo 文件...
   ✅ 移动: logo_package1.png
   ✅ 移动: logo_package2.png
   📊 共移动 4 个 logo 文件

📄 移动 HTML 报告...
   ✅ 移动: mcp_test_report_xxx.html
   ✅ 移动: agent_chat_test_xxx.html
   📊 共移动 14 个报告文件

✅ 整理完成！
📁 输出目录结构:
   outputs/
   ├── logos/    (4 个文件)
   └── reports/  (14 个文件)
```

## 🔍 查找文件

### 查看所有 Logo

```bash
# Windows
dir outputs\logos\

# Linux/Mac
ls outputs/logos/
```

### 查看所有报告

```bash
# Windows
dir outputs\reports\

# Linux/Mac
ls outputs/reports/
```

### 查找特定包的 Logo

```bash
# Windows
dir outputs\logos\logo_*package-name*.png

# Linux/Mac
ls outputs/logos/logo_*package-name*.png
```

### 查找最新的测试报告

```bash
# Windows
dir outputs\reports\ /O-D | Select-Object -First 5

# Linux/Mac
ls -lt outputs/reports/ | head -5
```

## 📊 文件管理建议

### 1. 定期清理

虽然这些文件不会提交到 Git，但建议定期清理旧的测试报告：

```bash
# 删除 7 天前的报告（Windows）
Get-ChildItem outputs\reports\*.html -File | Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-7) } | Remove-Item

# 删除 7 天前的报告（Linux/Mac）
find outputs/reports/ -name "*.html" -mtime +7 -delete
```

### 2. 备份重要报告

对于重要的测试报告，建议备份到其他位置：

```bash
# 创建备份目录
mkdir -p backups/reports

# 复制重要报告
cp outputs/reports/mcp_test_report_xxx.html backups/reports/
```

### 3. 分享测试报告

测试报告会自动上传到 EdgeOne CDN，你可以：
- ✅ 直接分享 EdgeOne 链接给团队成员
- ✅ 嵌入到文档或 README 中
- ✅ 用于演示和展示

## 🚀 自动化流程

### MCP 工厂完整流程

```
1. 选择项目 → 点击"开始生产"
2. GitHub 发布
3. PyPI/NPM 发布
4. 生成 Logo → 保存到 outputs/logos/
5. 发布到 EMCP
6. MCP 测试 → 生成报告到 outputs/reports/
7. Agent 测试 → 生成报告到 outputs/reports/
8. 完成！
```

所有输出文件都会自动保存到正确的位置，无需手动管理。

## ⚙️ 技术实现

### 自动创建目录

所有模块在保存文件前都会自动创建目录：

```python
from pathlib import Path

# 确保目录存在
logos_dir = Path("outputs/logos")
logos_dir.mkdir(parents=True, exist_ok=True)
```

### Logo 保存逻辑

**文件路径**：`src/jimeng_logo_generator.py`

```python
# 保存到 outputs/logos/
safe_name = re.sub(r'[/\\:*?"<>|@]', '_', package_name)
filename = logos_dir / f"logo_{safe_name}.png"
```

### 报告保存逻辑

**MCP 测试报告**：`src/mcp_tester.py`

```python
# 保存到 outputs/reports/
reports_dir = Path("outputs/reports")
output_path = reports_dir / "mcp_test_report_xxx.html"
```

**Agent 对话报告**：`src/signalr_chat_tester.py`

```python
# 保存到 outputs/reports/
reports_dir = Path("outputs/reports")
output_path = reports_dir / "agent_chat_test_xxx.html"
```

## 📝 .gitignore 配置

确保 `.gitignore` 包含以下内容：

```gitignore
# 输出文件（Logo 和报告）
outputs/
*.html
logo_*.png
```

这样可以防止：
- ✅ 输出目录被提交到 Git
- ✅ 散落的 HTML 报告被提交
- ✅ Logo 文件被提交

## ❓ 常见问题

### Q1: outputs 目录可以删除吗？

A: 可以！这个目录会在需要时自动创建。如果你想清理磁盘空间，可以直接删除：

```bash
# Windows
Remove-Item -Recurse outputs

# Linux/Mac
rm -rf outputs
```

### Q2: 可以更改输出目录吗？

A: 可以修改代码中的路径配置，但不建议这样做。统一使用 `outputs/` 目录可以保持项目的一致性。

### Q3: 为什么还有 .html 和 logo_*.png 在 .gitignore 中？

A: 这是为了兼容旧代码和防止散落的文件被提交。即使有文件不小心保存到根目录，也不会被提交到 Git。

### Q4: EdgeOne 链接会过期吗？

A: EdgeOne 链接是持久的，不会过期。但如果 EdgeOne 服务不可用，本地的 HTML 文件仍然可以查看。

### Q5: 可以自定义文件名吗？

A: Logo 文件名基于包名自动生成。测试报告的 ID 是随机生成的，确保唯一性。一般不需要自定义。

## 🎉 总结

新的文件管理系统让项目更加整洁和专业：

- ✅ **自动化**：无需手动管理，一切自动完成
- ✅ **规范化**：统一的目录结构和命名规则
- ✅ **Git 友好**：不会污染版本控制
- ✅ **易于维护**：集中管理，方便查找和清理

---

**Made with ❤️ by BACH Studio**

