name: Build and Release

on:
  push:
    tags:
      - 'v*'  # 只在推送 tag 时触发
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        include:
          - os: windows-latest
            artifact_name: RepoFlow-Windows.exe
            build_name: RepoFlow.exe
          - os: macos-latest
            artifact_name: RepoFlow-macOS
            build_name: RepoFlow
          - os: ubuntu-latest
            artifact_name: RepoFlow-Linux
            build_name: RepoFlow
    
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30  # 增加超时时间到 30 分钟
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Set UTF-8 encoding (Windows)
        if: runner.os == 'Windows'
        run: |
          chcp 65001
        shell: cmd
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
        env:
          PIP_NO_INPUT: 1
      
      - name: Build with PyInstaller
        run: python build_exe.py
        env:
          PYTHONIOENCODING: utf-8
      
      - name: Check built files
        run: ls -la dist/
        shell: bash
      
      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/RepoFlow.exe
      
      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/RepoFlow.app
      
      - name: Upload artifact (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/RepoFlow
  
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')  # 只有推送 tag 才创建 Release
    
    permissions:
      contents: write
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
      
      - name: List downloaded files
        run: |
          echo "Downloaded artifacts:"
          ls -R
      
      - name: Prepare release files
        run: |
          mkdir -p release
          
          # Windows
          if [ -f "RepoFlow-Windows.exe/RepoFlow.exe" ]; then
            cp RepoFlow-Windows.exe/RepoFlow.exe release/RepoFlow-Windows.exe
            echo "✓ Windows file prepared"
          fi
          
          # macOS (.app 打包成 zip)
          if [ -d "RepoFlow-macOS/RepoFlow.app" ]; then
            (cd RepoFlow-macOS && zip -r ../release/RepoFlow-macOS.zip RepoFlow.app)
            echo "✓ macOS .app zipped"
          elif [ -f "RepoFlow-macOS/RepoFlow" ]; then
            # 如果是单文件，直接复制并添加执行脚本说明
            cp RepoFlow-macOS/RepoFlow release/RepoFlow-macOS
            chmod +x release/RepoFlow-macOS
            echo "✓ macOS file prepared (single file)"
          fi
          
          # Linux
          if [ -f "RepoFlow-Linux/RepoFlow" ]; then
            cp RepoFlow-Linux/RepoFlow release/RepoFlow-Linux
            chmod +x release/RepoFlow-Linux
            echo "✓ Linux file prepared"
          fi
          
          echo "Release files:"
          ls -la release/
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          fail_on_unmatched_files: false
          body: |
            ## RepoFlow ${{ github.ref_name }}
            
            ### 📥 下载说明
            
            - **Windows**: RepoFlow-Windows.exe
            - **macOS**: RepoFlow-macOS.zip（解压后双击 RepoFlow.app）
            - **Linux**: RepoFlow-Linux（需要添加执行权限）
            
            ### 🚀 使用方法
            
            **Windows:**
            1. 下载 RepoFlow-Windows.exe
            2. 双击运行
            
            **macOS:**
            1. 下载 RepoFlow-macOS.zip
            2. 解压得到 RepoFlow.app
            3. 双击运行（第一次需要右键→打开）
            
            **Linux:**
            ```bash
            chmod +x RepoFlow-Linux
            ./RepoFlow-Linux
            ```
            
            ### 📦 包命名规范
            
            - PyPI: `bachai-{项目名}`
            - NPM: `@bachai/{项目名}`
            
            ### 📝 更新内容
            
            请查看提交记录了解详细更新内容。
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

